# Frontend - AI Mentor

React + Vite + Tailwind CSS.

## Setup
1. `npm install`
2. `npm run dev`

## Build
`npm run build` -> output in `dist/`.

## Configuration
Check `src/api.js` to configure Backend URL.
Currently set to `http://127.0.0.1:8001`.
